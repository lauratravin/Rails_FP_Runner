<% @users.order(:member_num).each do |u|%>
<%= u.id%> |
<%= u.member_num%> |
<%= u.name%> |
<%= u.dob%> |
<%= u.pace%> minutes per mile |
<%= u.uid%> |
<%= u.registrations.count%> |
<%= link_to "Edit", edit_user_path(u.id)%> 
<%= link_to "Show", user_path(u.id)%> <br>

<% end %>

<%= link_to "Console", admin_path%>